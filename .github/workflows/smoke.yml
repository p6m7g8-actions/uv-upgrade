name: Smoke

on:
  workflow_dispatch: {}

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: p6m7g8-actions/checkout@main

      - name: Run uv-upgrade action
        uses: ./
        with:
          gh_token: ${{ github.token }}
